import { StringsJoinerPipe } from './strings-joiner.pipe';

describe('StringsJoinerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringsJoinerPipe();
    expect(pipe).toBeTruthy();
  });
});
